exit
n
exit
p a
exit
eixt
p a
exit
end
# end
# def reveal
# end
# def won?
# end
# def render
end
    @grid[(0...@size).to_a.sample][(0...@size).to_a.sample] ||=Card.new(letter)
    letter = @@letters.split("").sample
     debugger
    while @grid.any? {|arr| arr.any? {|ele| ele==nil}}
def populate
end
@grid = Array.new(@size) {Array.new(@size)}
@size=size
def initialize(size)
@@letters = "ABCD"
class Board
require "byebug"
require_relative = "Card.rb"
p a
n
display letter
n
display @grid
